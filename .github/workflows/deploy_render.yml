name: Deploy to Render

on:
  push:
    branches: [ main, master ]

jobs:
  trigger-deploys:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger frontend deploy on Render
        if: ${{ secrets.RENDER_FRONTEND_SERVICE_ID }}
        run: |
          curl -s -X POST "https://api.render.com/v1/services/${{ secrets.RENDER_FRONTEND_SERVICE_ID }}/deploys" \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'

      - name: Trigger backend deploy on Render
        if: ${{ secrets.RENDER_BACKEND_SERVICE_ID }}
        run: |
          curl -s -X POST "https://api.render.com/v1/services/${{ secrets.RENDER_BACKEND_SERVICE_ID }}/deploys" \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'

      - name: Trigger ml-service deploy on Render
        if: ${{ secrets.RENDER_ML_SERVICE_ID }}
        run: |
          curl -s -X POST "https://api.render.com/v1/services/${{ secrets.RENDER_ML_SERVICE_ID }}/deploys" \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'
